apiVersion: 1
 
providers:
  - name: 'Azion Metrics Datasets'
    folder: 'Data Platform'
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/datasets
  - name: 'L2 Cache Examples'
    folder: 'Data Platform'
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/l2Cache

